apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kws-prod-ingress
  namespace: kws-prod
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kws-prod-service
                port:
                  number: 80
